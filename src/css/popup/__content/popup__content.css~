.popup__content {
    max-width: 430px;
    width: 100%;
    min-height: 330px;
    background-color: #fff;
    border-radius: 10px;
    position: relative;
    box-sizing: border-box;
    padding: 34px 36px;
}

.popup__content_avatar {
    min-height: 272px;
}

@media screen and (max-width: 568px) {
    .popup__content {
        width: 100%;
        max-width: calc(100% - 80px);
        margin-top: 40px;
        padding: 30px 20px;
    }

    .popup__content_avatar {
        height: 272px;
    }
}

.popup__content_avatar {
    height: 272px;
}






/* Container for each input+error pair */
.popup__field-group {
    position: relative;
    margin-bottom: 20px;
}

/* Error message positioning */
.popup__error {
    position: absolute;
    bottom: -18px;
    left: 0;
    color: red;
    font-size: 12px;
    line-height: 15px;
    opacity: 0;
    transition: opacity 0.3s;
}

.popup__error_visible {
    opacity: 1;
}

/* Remove any existing margin/padding that might interfere */
.popup__input {
    margin-bottom: 5px;
}